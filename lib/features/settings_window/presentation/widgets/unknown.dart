import 'package:flutter/material.dart';

class UnknownSettingsRoute extends StatelessWidget {
  const UnknownSettingsRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Not found'),
    );
  }
}
